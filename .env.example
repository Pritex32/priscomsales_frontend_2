# Public keys only - these are safe to expose in frontend
REACT_APP_MONO_PUBLIC_KEY=test_pk_your_public_key_here

# API Configuration
REACT_APP_API_URL=http://localhost:8000

# NOTE: NEVER put secret keys (sk_test_, sk_live_, jwt secrets) in frontend!
# Secret keys belong in backend/.env only
# Only public keys (pk_test_, pk_live_) are safe for frontend
